import { Chain } from '@particle-network/chains';
import { Auth } from './auth';
import EvmService from './service/evmService';
import SolanaService from './service/solanaService';
import { AuthThemeConfig, Config, CurrencyUnit, EVMProvider, OpenBuyOptions, SolanaWallet, Wallet } from './types';
import { WalletConfig } from './utils/wallet-url';
declare global {
    interface Window {
        particle?: ParticleNetwork;
        __PARTICLE_ENVIRONMENT__?: string;
        __PARTICLE_AUTH_LOCALHOST__?: string;
        ethereum?: any;
        solana?: any;
    }
}
export declare class ParticleNetwork {
    #private;
    readonly isParticleNetwork = true;
    readonly auth: Auth;
    private readonly config;
    particleProvider: EVMProvider;
    localProvider: EVMProvider;
    solanaWallet: SolanaWallet;
    private walletEntry?;
    evm: EvmService;
    solana: SolanaService;
    constructor(config?: Config);
    private preload;
    private openActive;
    setAuthTheme(config: AuthThemeConfig): void;
    getAuthTheme(): AuthThemeConfig;
    switchChain(chain: Chain, hideLoading?: boolean): Promise<Wallet[]>;
    setChainInfo(chain: Chain): void;
    walletEntryDestroy(): void;
    walletEntryCreate(): void;
    /**
     * open wallet web wallet
     */
    openWallet(): void;
    buildWalletUrl(config?: WalletConfig): string;
    openBuy(options?: OpenBuyOptions, target?: string, features?: string): void;
    setLanguage(code: string): void;
    getLanguage(): string;
    get version(): string;
    setFiatCoin(unit: CurrencyUnit): void;
    getChain(): Chain;
    /**
     * ERC-4337: Account Abstraction Using Alt Mempool
     * @param enable Whether to enable the 4337 mode
     * @see https://eips.ethereum.org/EIPS/eip-4337
     */
    setERC4337(enable: boolean): void;
}
